# Local, private, never committed: real account data and session material.
endpoints.json
mirror/*
!mirror/.gitkeep
# Real fact sheets carry live listing ids + private price floors — only the template is tracked.
listings/*
!listings/example.yaml
*.har
cookies*
*.cookies
