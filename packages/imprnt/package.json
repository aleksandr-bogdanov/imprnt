{
  "name": "imprnt",
  "version": "0.1.0",
  "description": "Deterministic-first, plain-markdown knowledge vault. Code does the bulk transform. The LLM only touches the irreducibly-semantic 20%. Native grep plus BM25 retrieval, zero MCP on the vault.",
  "type": "module",
  "bin": {
    "imprnt": "dist/cli.js"
  },
  "scripts": {
    "build": "bun build scripts/cli.ts --target=node --outfile=dist/cli.js --banner '#!/usr/bin/env node' && chmod +x dist/cli.js",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "shipdocs": "cp ../../CLAUDE.md ../../README.md ../../LICENSE .",
    "prepublishOnly": "bun run shipdocs && bun run typecheck && bun run test && bun run build"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "author": "Aleksandr Bogdanov",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aleksandr-bogdanov/imprint.git",
    "directory": "packages/imprnt"
  },
  "keywords": [
    "knowledge-base",
    "markdown",
    "second-brain",
    "deterministic",
    "cli",
    "agent-context"
  ],
  "files": [
    "dist",
    "templates",
    "CLAUDE.md",
    "README.md",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.2"
  }
}
