{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "tasks": {
    "build": {
      "inputs": ["src/**", "scripts/**", "templates/**", "package.json", "tsconfig.json"],
      "outputs": ["dist/**", "check.js", "timemachine.js", "statusline.js", "kopeika.js", "kleinanzeigen.js", "session-host.js", "client.js"]
    },
    "typecheck": {
      "inputs": ["src/**", "scripts/**", "package.json", "tsconfig.json"],
      "outputs": []
    },
    "test": {
      "inputs": ["src/**", "scripts/**", "templates/**", "package.json", "tsconfig.json"],
      "outputs": []
    }
  }
}
