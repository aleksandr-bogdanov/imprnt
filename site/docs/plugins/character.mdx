---
title: Character
description: A voice and standards your assistant works in, so it acts like itself, not generic Claude.
---

import { Steps } from "@astrojs/starlight/components";
import CharacterSets from "../../src/components/docs/CharacterSets.astro";
import CharacterScribe from "../../src/components/docs/CharacterScribe.astro";
import CharacterTouches from "../../src/components/docs/CharacterTouches.astro";

A **character** file gives your assistant a personality, a voice, and a set of standards, so it works like a specific person instead of generic Claude. It sets three things:

<CharacterSets />

This is the most load-bearing plugin, because the character file is how an assistant's identity survives moving between systems and sessions. The package ships one default you can use as is or make your own.

<CharacterScribe />

## How it works

<CharacterTouches />

A character generalizes to one or many. One ships today (Scribe), and each is its own file in the folder. There is no referee. Wire in two characters that contradict each other and you reconcile them. That is the cost of choosing who is on.

## Install

<Steps>

1. **Add the plugin.** One command copies the package in and wires it into your sessions.

   ```sh
   imprnt plugin add character
   ```

   That copies the package into `plugins/character/` and wires `@plugins/character/agent.md` into `CLAUDE.local.md`, your per-machine on-off file. You can also hand-edit that file and add the line yourself.

2. **Make it yours.** Scribe is a generalized default. Copy it into the private `_personal/` folder, edit the voice, and wire that copy instead. `_personal/` is gitignored, so your private character never ships and never lands in the public gallery.

</Steps>

<div class="remove-block">
  <span class="remove-label">Remove</span>

```sh
imprnt plugin rm character
```

</div>

<style>{`
  .sl-markdown-content p code {
    padding-block: 0.05em;
    margin-block: -0.05em;
  }
  .remove-block {
    margin-top: 1.4rem;
  }
  .remove-block .remove-label {
    display: block;
    font-family: var(--sl-font-mono);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--sl-color-gray-2);
    margin-bottom: 0.5rem;
  }
  .remove-block .expressive-code,
  .remove-block pre {
    margin-top: 0;
    margin-bottom: 0;
  }
  /* Finding 1: tighten the oversized content-to-pagination gap on this short page.
     The void stacks the container's sibling margin (1.5rem), Starlight's empty
     footer .meta (margin-top: 3rem), and the footer column gap (1.5rem) above the
     pagination. Halve it page-locally. Unlayered rules win over Starlight's @layer
     styles, so no !important needed. */
  .sl-container > footer.sl-flex {
    margin-top: 0.6rem;
  }
  footer.sl-flex .meta:empty {
    margin-top: 1.2rem;
  }
  footer.sl-flex {
    gap: 0.9rem;
  }
  @media (max-width: 640px) {
    /* Finding 2: let long path chips shrink and break inside so the sentence
       keeps flowing instead of each chip forcing its own line. */
    .sl-markdown-content p code {
      font-size: 0.82em;
      overflow-wrap: anywhere;
      word-break: break-word;
    }
  }
`}</style>
