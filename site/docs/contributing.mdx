---
title: Contributing
description: How imprnt is built, structured, and released, for anyone hacking on it.
---

import ContributingFactStrip from "../src/components/docs/ContributingFactStrip.astro";
import ContributingPackages from "../src/components/docs/ContributingPackages.astro";
import ContributingBuildFlow from "../src/components/docs/ContributingBuildFlow.astro";
import ContributingPrivacySplit from "../src/components/docs/ContributingPrivacySplit.astro";
import ContributingDistTags from "../src/components/docs/ContributingDistTags.astro";
import ContributingReleaseFlow from "../src/components/docs/ContributingReleaseFlow.astro";
import ContributingSafeguards from "../src/components/docs/ContributingSafeguards.astro";
import ContributingNext from "../src/components/docs/ContributingNext.astro";

This page is for people building imprnt itself. imprnt is open source under the [MIT license](https://github.com/aleksandr-bogdanov/imprnt/blob/master/LICENSE).

<ContributingFactStrip />

## The monorepo

Everything lives in one git repository: the core and every plugin, wired together with [bun workspaces](https://bun.sh/docs/install/workspaces) so one `bun install` sets up the whole thing and you develop it all side by side. At publish time the one repo splits into two package families.

<ContributingPackages />

```sh
bun install
bun run build      # compile each package
bun run test       # test all packages
bun run check      # typecheck + test + build
```

## Ship Node, build with Bun

What a user installs runs on **Node**, the runtime everyone already has, so `npm i -g imprnt` works without making anyone install Bun first. A code plugin compiles the same way, so running plugin code needs only Node too.

<ContributingBuildFlow />

## Generic ships, personal stays private

<ContributingPrivacySplit />

## Releasing

imprnt publishes to npm on two [dist-tags](https://docs.npmjs.com/cli/v10/commands/npm-dist-tag): named pointers that tell npm which version of the one imprnt package to install.

<ContributingDistTags />

Every package versions independently. [Turborepo's](https://turborepo.com) `--affected` works out which packages a change actually touched, so a package whose files did not change is never republished. A core installed from `edge` pulls its plugins from `edge` too, so the newest core gets exercised against the newest plugins automatically.

All publishing runs through one GitHub Actions workflow with two paths:

<ContributingReleaseFlow />

Three safeguards sit on that workflow:

<ContributingSafeguards />

Trusted publishing is configured once per package on npmjs.com.

To cut a stable release, use the `imprnt-release` skill (it fires the workflow and reports), or run it by hand:

```sh
# fire the release, then approve the npm-production gate
gh workflow run publish.yml -f mode=release

# plan only, publishes nothing
gh workflow run publish.yml -f mode=release -f dry_run=true
```

npm only allows a full unpublish within 72 hours, so for a normal release you cannot count on it. To undo a bad `latest`, point the tag back to the last good version and deprecate the bad one:

```sh
npm dist-tag add imprnt@<last-good> latest
npm deprecate imprnt@<bad> "broken release, use <last-good>"
```

## Read next

<ContributingNext />

{/* Page-scoped only (ships on /contributing/ alone): on narrow screens the
    always-visible expressive-code copy button overlaps the first code line.
    Reserve room above the first line so the button never covers code. */}
<style is:global>
  {`
  @media (max-width: 40rem) {
    .sl-markdown-content .expressive-code pre > code {
      padding-top: 1.9rem;
    }
    /* A command line wider than the phone scrolls horizontally with no scrollbar,
       so the right edge looks like a hard cut. Fade the last ~2rem of every code
       block to read as "more to the right, scroll it" instead of truncated. The
       mask only dims the trailing edge, so a line that fits is untouched. */
    .sl-markdown-content .expressive-code pre {
      -webkit-mask-image: linear-gradient(to right, #000 calc(100% - 2rem), transparent);
      mask-image: linear-gradient(to right, #000 calc(100% - 2rem), transparent);
    }
  }
  /* Page-scoped: tighten the closing rhythm so the page bottom matches the body.
     The "Read next" H2 sits after the last npm code block, and Starlight's
     Previous/Next pager follows the cards. Both carried oversized gaps. */
  .sl-markdown-content #read-next {
    margin-top: 1.8rem;
  }
  .sl-markdown-content #read-next + * {
    margin-top: 0.6rem;
  }
  /* Page-scoped: the content footer holds an EMPTY .meta div that still carries
     margin-top: 3rem, and the footer flex container adds gap: 1.5rem before the
     Previous/Next pager. Together they leave a dead whitespace band between the
     Read-next cards and the pager. Collapse the empty meta and zero the footer
     gap so the pager follows the cards on the same ~1.6rem rhythm as the rest of
     the page. Spacing the pager via a margin (not the footer flex gap) is what
     actually lands, since the gap is what created the band. */
  .sl-markdown-content + footer {
    margin-top: 1.6rem;
    gap: 0;
  }
  .sl-markdown-content + footer .meta:empty {
    margin-top: 0;
    display: none;
  }
  .pagination-links {
    margin-top: 0;
  }
  /* Page-scoped: on narrow screens the Read-next cards stack full-width. The
     footer/.meta collapse above closes the residual mobile band too, so the
     pager sits ~1.6rem below the last card. */
  @media (max-width: 50rem) {
    .sl-markdown-content #read-next + * {
      margin-top: 0.6rem;
      margin-bottom: 1.6rem;
    }
  }
  `}
</style>
