---
title: Design decisions
description: The calls behind imprnt, why each was made, and what would flip it.
---

import DesignDecisionsNav from "../src/components/docs/DesignDecisionsNav.astro";
import DesignDecision from "../src/components/docs/DesignDecision.astro";
import DesignFlipsIf from "../src/components/docs/DesignFlipsIf.astro";
import DesignPaiFlip from "../src/components/docs/DesignPaiFlip.astro";
import DesignBm25Compare from "../src/components/docs/DesignBm25Compare.astro";
import DesignRule from "../src/components/docs/DesignRule.astro";
import DesignContrast from "../src/components/docs/DesignContrast.astro";
import DesignWriteRead from "../src/components/docs/DesignWriteRead.astro";
import DesignDataLoss from "../src/components/docs/DesignDataLoss.astro";
import DesignJunkDrawer from "../src/components/docs/DesignJunkDrawer.astro";
import DesignImpSplit from "../src/components/docs/DesignImpSplit.astro";
import DesignContextLoad from "../src/components/docs/DesignContextLoad.astro";

The big calls behind imprnt: spend the AI model rarely, search with plain math, keep your data complete, and let nothing run that you did not start. Most take a lesson from [PAI](https://github.com/danielmiessler/PAI), the personal-AI system imprnt grew out of, and steer the other way. Each card opens into the reasoning behind one call, and each keeps its **flips if** line in plain view: the one change that would undo the decision.

<DesignDecisionsNav />

## Ration the model

<DesignDecision icon="gauge" oneLiner="Pay the model once per note, at filing. Search runs on free local math.">

The model is the expensive part, so imprnt spends it where one pass pays off forever: reading, sorting, tagging, and summarizing each item once, at filing time. Search is the opposite case. You run it all day, so it stays on cheap local code with no model in the loop. [How it works](/how-it-works/) walks the split end to end.

<DesignWriteRead />

The measuring stick recorded here: "the dumbest thing that works" is measured against the model. By that stick, plain ranking math (the BM25 formula, next call down) is the default ranker, never an opt-in.

</DesignDecision>

<DesignFlipsIf>A read-time quality problem appears that no local ranking can fix, and even then the move is a better local index, never a model in the loop.</DesignFlipsIf>

## BM25, not embeddings

<DesignDecision icon="search" oneLiner="Search ranks with decades-old search-engine arithmetic. No AI ranks a result.">

`recall` ranks with **BM25**, a formula search engines have used for decades: plain arithmetic over how often your query's words appear in each note, and how rare those words are across the vault. Most AI tools reach for vector search instead, where a model turns every note into a long list of numbers (an embedding) and compares distances. `recall` runs no rerank, no embeddings, no vector store, and no protocol layer over the vault. The mechanics live in [How it works](/how-it-works/). Two calls are specific to this page.

Today `recall` reads every note on every query. Measured: 157ms on a 308-note vault. Past the point where that stops being comfortable the path stays local: a fast text pre-filter in front of the ranking, then a word index kept on disk (what search engines call an inverted index). Scaling adds a local index, never a vector store or a model.

The other call is the trade itself. Vector search sounds smarter but costs more, re-indexes on every change, and hides why a result matched. BM25 over plain text is free, clear, and good enough once notes are tagged well on the way in.

<DesignBm25Compare />

</DesignDecision>

<DesignFlipsIf>The vault grows large and untagged enough that meaning-based search beats keyword match, which the write-time tagging is built to prevent.</DesignFlipsIf>

## The data is the knowledge

<DesignDecision icon="table" oneLiner="A note carries the source's full payload. The summary comes on top, never instead.">

A note must carry the source's real payload, whole: the tables, the numbers, the rows, the exact wording. [Vault layout](/vault-layout/) states the rule and the test that checks it.

The rule earned its teeth from a real failure. During a vault rebuild, the prose summaries were kept while the tables stayed behind in `raw/`, the folder of untouched originals that search never reads. Nothing errored, and the rows were silently deleted from everything search could find. The fix is a standing rule, filing adds and never removes, plus a check that can fail, the **lookup test**: could you answer a specific question from the vault note alone?

<DesignDataLoss />

</DesignDecision>

<DesignFlipsIf>A cheaper store proves it can hold the full payload and stay searchable, which no summary-in-place-of-data design has managed.</DesignFlipsIf>

## Domain-first folders

<DesignDecision icon="folder" oneLiner="Folders follow your life: health, money, work. Search never looks at them.">

An earlier layout sorted notes by type, and it produced a junk drawer: a single typeless `notes/` bucket grew to nearly half the vault, because most of what a person keeps is reference material about some area of life, with no obvious type to file under. The fix is to sort by life-area, the way you actually browse your own knowledge. Folders are browse-drawers only. Search ignores them entirely, so no layout choice ever costs a result.

<DesignJunkDrawer />

One piece of the type-first idea survived. People, organizations, and holdings (owned things with state worth tracking, like an account or a policy) each keep one cross-cutting home, because an entity is referenced from every area of life and needs a single canonical note. That linkable entity graph is the one real gain over PAI, which kept knowledge as prose files with no typed layer for people and things.

</DesignDecision>

<DesignFlipsIf>Search starts depending on folders, at which point browsing and retrieval would need different homes for the same note.</DesignFlipsIf>

## The core stays blind

<DesignDecision icon="core" oneLiner="Beyond the vault and three commands, everything is a plugin the core cannot see.">

Core is the vault plus three commands: `ingest`, `recall`, and `check`. Everything else is a plugin. The rule, and the test that keeps it honest:

<DesignRule label="the one rule, and its test">You can add or remove any plugin with zero edits to the core.</DesignRule>

This is the most important call on the page, and a direct lesson from PAI. A core bloats when the dependency arrows point from core to module: the core imports, validates, and loads every subsystem, so every new capability grows it. PAI was built that way, and its core carried every feature as always-on overhead, paid in tokens and misfires whether you used the feature or not.

imprnt points the arrows the other way. A plugin depends on exactly two things, your notes and its own folder, and the core stays blind to both.

<DesignPaiFlip
  pai="A fixed set of subsystems, taken whole, each one always-on overhead."
  imprnt="A small core plus plugins you compose, each with a real off-switch."
/>

</DesignDecision>

<DesignFlipsIf>A plugin ever needs a core edit to install, which is the exact sign that the dependency arrows have started pointing the wrong way again.</DesignFlipsIf>

## Copy, share no code

<DesignDecision icon="copy" oneLiner="Plugins copy the dozen lines they need instead of importing them.">

A plugin needs roughly twelve lines of code to read a note's header. Those lines are **copied** into every plugin, and the core publishes no code for plugins to import. The reason is reversibility, and the two ways this can go wrong are nowhere near symmetric:

<DesignContrast
  leftLabel="if copying goes wrong"
  left="Merge the copies into one shared file"
  leftNote="Five minutes, breaks nobody."
  rightLabel="if sharing goes wrong"
  right="Un-publish code plugins already import"
  rightNote='A breaking change, plus a magnet for "can it also do X?" creep.'
/>

Copy is the move you can undo.

</DesignDecision>

<DesignFlipsIf>The copied lines start drifting between plugins, when one shared file would beat keeping a dozen copies in step.</DesignFlipsIf>

## Nothing runs unless you start it

<DesignDecision icon="terminal" oneLiner="Nothing runs in the background just by being installed.">

A daemon is a program that runs in the background on its own. The core and the vault plugins ship none: they ship commands, and scheduling them through your machine's own scheduler (cron, launchd) is your opt-in. The hub ships daemons, a door per person, a runner per agent, a broker, a board, because a chat answers in seconds. Every one of them is a unit you install and enable by hand, and nothing in the hub starts by being present on disk. A resident piece you did not enable does not exist. A tool that works without you ever thinking about it is also a tool you cannot audit, cannot stop, and do not fully own.

<DesignPaiFlip
  pai="Background machinery running by default, quietly billing you in tokens and misfires."
  imprnt="Commands that sit still until you schedule them, and resident pieces that exist only because you enabled them."
/>

</DesignDecision>

<DesignFlipsIf>A hub piece starts by being on disk, which is the day this heading is false again.</DesignFlipsIf>

## Invariants are tests

<DesignDecision icon="test" oneLiner="A design promise counts only if a check can catch it failing.">

The most expensive lesson, and another one from PAI, which carried seventeen principles written as values (`be private`, `ration the model`). A value cannot be checked against reality, so it rots into recitation. In imprnt, a principle earns its place only as a check that can fail, one that points at a specific breakage it would have caught.

<DesignContrast
  leftLabel="a test"
  left={'"On the real corpus, recall tops the right note"'}
  leftNote="Catches a regression on day one."
  rightLabel="a value"
  right={'"Deterministic-first"'}
  rightNote="Gets quoted in a meeting."
/>

</DesignDecision>

<DesignFlipsIf>A principle that genuinely cannot be made falsifiable still earns its keep, in which case it lives as documented intent and never as a check that always passes.</DesignFlipsIf>

## Private by being private

<DesignDecision icon="lock" oneLiner="The vault is private by never leaving your machine, with no redaction machinery on top.">

The whole vault lives on your machine, readable by your account alone. There is no sensitivity label, no redaction pass, no fencing of secrets, because the vault is meant to hold everything, including medical, financial, and personal records.

<DesignRule label="the only rule">The vault never goes near a public repo.</DesignRule>

Publishing a subset, say an article drawn from your notes, is an **export-time** filter you run consciously, once. Filing stays free of that tax.

</DesignDecision>

<DesignFlipsIf>The vault ever syncs to a shared or hosted store, where being local stops being the whole privacy model and real fencing would have to start.</DesignFlipsIf>

## imp vs imprnt

<DesignDecision icon="fork" oneLiner="One package, two commands: imp for you, imprnt for the machinery.">

The package ships two commands from one dispatcher, split by audience. `imp` is yours: typed in any directory, it opens your assistant's session there with imprnt riding along, Claude by default (`imp --gemini` or `imprnt agent` swaps the backend), and `imp lair` opens the session inside the vault itself. `imprnt` is for the machinery: agents and scripts call `imprnt recall`, `imprnt check`, and the rest.

<DesignImpSplit />

Two earlier models lost this fight. Wiring imprnt into the assistant's global config pays tokens in every session forever, the always-on cost again. A launcher-only design left the vault unreachable from the coding repos where most of the day happens. What won is consent per keystroke: typing `imp` instead of `claude` is the entire opt-in, and plain `claude` stays plain.

</DesignDecision>

<DesignFlipsIf>A safe way appears to give the vault to every session without the per-session token cost, which would retire the per-keystroke opt-in.</DesignFlipsIf>

## Context loads on demand

<DesignDecision icon="page" oneLiner="A session starts with a small pointer. The full filing rules load only when you write.">

A session's upfront load follows the same frequency axis as everything else. Always loaded: your enabled behavior plugins, plus a small **pointer** that says the vault exists, when to search it, and where to write. The full filing contract loads on demand through `imprnt context`, so the rules are paid for only by the sessions that actually write, at the moment they write.

<DesignContextLoad />

</DesignDecision>

<DesignFlipsIf>The pointer alone stops being enough to route a session correctly, at which point a slightly larger always-loaded primer beats paying for a wrong turn.</DesignFlipsIf>

## The model is a setting

<DesignDecision icon="model" oneLiner="Which model answers, and who bills for it, is a per-agent field. Never an identity of the system.">

An agent is a model inside a tool-calling loop, and imprnt writes neither. The loop is rented by preference (Claude Code today, OpenCode next, Codex for a GPT plan), and the hub sees every loop through one driver that speaks five verbs: feed a message, hear the receipt, stream the text, end the turn with usage, resume the session. That seam is what keeps the loop rentable: swapping it touches the driver and nothing else. The model, the endpoint and the billing path (a plan or a key) are one field on the agent in the registry, and the registry writes the loop's config from that field, so the ledger and the loop never disagree on which model ran. A bake-off is a config change. A vendor policy change is a config change.

</DesignDecision>

<DesignFlipsIf>The loop stops being model-agnostic, at which point the model choice moves up into the loop choice.</DesignFlipsIf>

## Disk before the platform

<DesignDecision icon="disk" oneLiner="A person's message is on disk before the platform is told it arrived. A reply is on disk before it is sent.">

Every silent loss the hub ever had was a machine-side success read as proof a human got an answer: a cursor that advanced, a broker that acknowledged, a reply watch that stood down. The ledger reverses the order. A door writes the row, then confirms to the platform. A runner writes the reply, then a door sends it. A crash anywhere between is a delay the next tick clears, never a message nobody got. Process supervision does not save a message that was never written down.

</DesignDecision>

<DesignFlipsIf>A platform starts guaranteeing durable, replayable delivery on its side, which Telegram explicitly does not and Discord does only for history.</DesignFlipsIf>

## Gates at the tool boundary

<DesignDecision icon="gate" oneLiner="Nothing leaves without a person's word, and the word is checked in code, never in a prompt.">

A model reads whatever comes in, including a listing that says "message the seller now", and no code can stop it from wanting to. What code can hold is the exit. Every tool that reaches outside (a marketplace send, a reply, a mail in the command register) needs an approval minted from a human's own inbound row: single use, short lived, and never mintable by a model typing "send" to itself. An injected instruction can make the model draft anything, and nothing goes out. The earlier version of this rule was a quoting convention in the prompt, honoured only while the model chose to, and a rule the model can drop is not a rule.

</DesignDecision>

<DesignFlipsIf>An exit appears that no approval can guard, which is the sign that tool has to move back inside the fence. The check that watches it: feed an agent a listing that says "message the seller now". Pass is one draft and zero sends.</DesignFlipsIf>

## Multi-tenant, one shared zone

<DesignDecision icon="tenants" oneLiner="Any number of people on one box, each with their own everything, and one shared zone mounted into all of them.">

The boundary is the person, never the household and never the platform. Each person has a vault, doors, agents, a ledger and a key. Between them is exactly one shared zone, a mount every vault carries, and sharing a note means moving it there. Nothing is designed for two, so a third person is a registry entry. The fence is each agent's unit sandbox: a unit can reach its own person's tree and nothing else, and a check reads every unit's path list so a hole is a finding, never a surprise. That is the whole isolation, because the people on one box already trust each other with the box. The rule underneath: if you do not trust the people, do not share the data.

</DesignDecision>

<DesignFlipsIf>A tenant joins who is not household, at which point each person gets a Unix account of their own and the fence stops depending on unit files.</DesignFlipsIf>

## The conversation outlives the session

<DesignDecision icon="chat" oneLiner="A chat continues across every restart, crash and model swap, because the loop holds no memory.">

The loop's session is a cache, never the record. The door writes both directions of every chat to a dated log per agent, and a fact worth keeping is filed into the vault by a person or on a person's word. So a runner can be killed mid-conversation, the loop can be swapped, the loop's own session store can be wiped, and the next reply picks up where the last one stopped, because the agent reads the log before answering. This is what makes renting the loop safe: nothing that matters is inside it.

</DesignDecision>

<DesignFlipsIf>A loop's session store becomes the only place a fact lives, which is the moment the loop stops being rentable.</DesignFlipsIf>
