---
title: How plugins work
description: A tiny core plus add-ons you switch on by name. How they compose, and how they wire into Claude.
---

import PluginsKindsDiagram from "../src/islands/PluginsKindsDiagram.tsx";
import Rules from "../src/components/docs/Rules.astro";
import RuleBanner from "../src/components/docs/RuleBanner.astro";
import PluginsWiringTable from "../src/components/docs/PluginsWiringTable.astro";
import PluginsInstall from "../src/components/docs/PluginsInstall.astro";

The **core** is your notes plus three commands: ingest, recall, and check. Everything else is a **plugin** you add when you want it and remove with one line. The core finds plugins by the name of their folder and never depends on one (the only plugin names it carries are a display hint for `imprnt plugin list`).

Every plugin is one of **three kinds**, by how it reaches the assistant. The switches below are live: pull a kind out and watch what happens to the core.

<PluginsKindsDiagram client:load />

<RuleBanner />

<PluginsInstall />

## How a plugin reaches Claude

A plugin is a folder with one instruction file, `agent.md`, that tells the assistant what the plugin is and how to use it. Installing the plugin loads that file into your sessions, and **where it loads decides the reach**.

<PluginsWiringTable />

## Four rules keep the core blind

<Rules />
